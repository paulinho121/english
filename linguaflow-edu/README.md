# LinguaflowEdu 🎓

Bem-vindo ao LinguaflowEdu, a plataforma premium de gestão educacional da Linguaflow. Este projeto foi concebido para ser totalmente independente, focado em capacitar professores com ferramentas de gestão, AI insights e dashboards de performance.

## 🚀 Como Iniciar

1. Navegue até a pasta do projeto:
   ```bash
   cd linguaflow-edu
   ```

2. Instale as dependências:
   ```bash
   npm install
   ```

3. Inicie o servidor de desenvolvimento:
   ```bash
   npm run dev
   ```

## 🏗️ Arquitetura

- **Frontend**: React 19 + TypeScript + Vite.
- **Estilização**: Tailwind CSS com Design System Educacional Customizado (Indigo/Slate).
- **Animações**: Framer Motion para micro-interações premium.
- **Banco de Dados**: Schema `education` isolado no Supabase (ver `schema.sql`).
- **Icons**: Lucide React.

## 📂 Estrutura de Pastas

- `src/components`: Componentes de UI (Sidebar, Dashboards, Cards).
- `src/lib`: Configurações de serviços (Supabase).
- `schema.sql`: Definição das tabelas do banco de dados educacional.

---
*Desenvolvido com foco em excelência pedagógica e autoridade do professor.*
